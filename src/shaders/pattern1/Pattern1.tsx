import React from "react";
import vertexShader from "./vertex.glsl";
import fragmetShader from "./fragment.glsl";

type Props = {};

const Pattern1 = (props: Props) => {
  return <div>Pattern1</div>;
};

export default Pattern1;
